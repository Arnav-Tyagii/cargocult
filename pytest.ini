[pytest]
# Repo root on sys.path so tests can `from src.sandbox import ...` without an
# installed package.
pythonpath = .
testpaths = tests
markers =
    gpu: needs a CUDA device and the real policy model; deselect with -m "not gpu"
